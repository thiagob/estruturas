import importlib
import os
import thiagob as lib

# subfolders = [ f.path for f in os.scandir('.') if f.is_dir() ]

# library = "template"
# for f in subfolders:
#     if not f.startswith(".\."):
#         folder = f[2:]
#         if folder not in ["docs", "exercises", "template", "tests"]:
#             library = folder
#             print("SELECT LIBRABRY >>> " + library)

# lib = importlib.import_module(library)